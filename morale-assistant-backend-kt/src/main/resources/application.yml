spring:
  datasource:
    url: jdbc:postgresql://localhost:32768/moraleassistant
    username: postgres
    password:
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      ddl-auto: create-drop

sounds:
  basePath: /tmp/morale-assistant/

googleCredentialsFile: classpath:google_credentials.json
