server:
  servlet:
    context-path: "/api/"

spring:
  datasource:
    url: jdbc:postgresql://${DB_SERVER}/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

sounds:
  base-path: ${SOUNDS_BASE_PATH}

synthesizer:
  google:
    enabled: false
